---
'@artui/cli': patch
---

Point default registry URL at `artui.vandervennet.art` (was the unowned `artui.dev` placeholder). Fresh `artui init` now writes a `components.json` whose `registry` resolves.
