# @artui/mcp

## 0.0.1

### Patch Changes

- ba7c1fb: Default registry URL now points at `artui.vandervennet.art` instead of the unreachable `artui.dev`. `ARTUI_REGISTRY` override still works as before.
