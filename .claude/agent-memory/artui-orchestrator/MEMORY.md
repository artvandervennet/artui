# Orchestrator Memory Index

- [eslint-disable-next-line placement](feedback-lint-eslint-disable-placement.md) — directive must be the line immediately before the flagged element; NOTE comments between directive and element cause misfires
- [MCP server is data-driven](feedback-mcp-server-data-driven.md) — tools.ts reads registry.json dynamically; new components need no code changes to tools.ts
- [MCP lives in apps/docs — no @artui/mcp package](project-mcp-lives-in-docs.md) — do not list @artui/mcp in changesets; apps/docs is private
- [registry.json is regenerated by pnpm typecheck](project-registry-build-auto.md) — turbo runs the build script automatically; no separate pnpm build step needed
- [Changeset files can be written directly](project-changeset-convention.md) — write .changeset/<slug>.md directly; pnpm changeset is interactive and can't be automated
- [dialog/showModal() must never be conditionally unmounted](feedback-dialog-modal-always-mounted.md) — removing a <dialog> from DOM while in showModal() strands NVDA's virtual buffer; always keep mounted, split open/close into two effects
