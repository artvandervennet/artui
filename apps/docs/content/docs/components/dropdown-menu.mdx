---
title: DropdownMenu
description: Accessible dropdown menu following the WAI-ARIA APG Menu Button pattern. Single-level submenus, typeahead, full keyboard navigation, compile-time enforcement of accessible names.
---

```bash
npx artui@latest add dropdown-menu
```

## Playground

<DropdownMenuPlayground />

## Usage

### Basic

```tsx
import { DropdownMenu } from '@/components/dropdown-menu';

<DropdownMenu>
  <DropdownMenu.Trigger>Account</DropdownMenu.Trigger>
  <DropdownMenu.Content>
    <DropdownMenu.Item onSelect={() => navigate('/profile')}>Profile</DropdownMenu.Item>
    <DropdownMenu.Item onSelect={() => navigate('/settings')}>Settings</DropdownMenu.Item>
    <DropdownMenu.Separator />
    <DropdownMenu.Item onSelect={signOut}>Sign out</DropdownMenu.Item>
  </DropdownMenu.Content>
</DropdownMenu>
```

### With submenu

```tsx
<DropdownMenu>
  <DropdownMenu.Trigger>Account</DropdownMenu.Trigger>
  <DropdownMenu.Content>
    <DropdownMenu.Item onSelect={() => navigate('/profile')}>Profile</DropdownMenu.Item>
    <DropdownMenu.Sub>
      <DropdownMenu.SubTrigger>Preferences</DropdownMenu.SubTrigger>
      <DropdownMenu.SubContent>
        <DropdownMenu.Item onSelect={() => setTheme('light')}>Light theme</DropdownMenu.Item>
        <DropdownMenu.Item onSelect={() => setTheme('dark')}>Dark theme</DropdownMenu.Item>
      </DropdownMenu.SubContent>
    </DropdownMenu.Sub>
  </DropdownMenu.Content>
</DropdownMenu>
```

Arrow Right or click opens the submenu. Arrow Left or Escape closes it and returns focus to the SubTrigger.

### Controlled

```tsx
const [open, setOpen] = useState(false);

<DropdownMenu open={open} onOpenChange={setOpen}>
  <DropdownMenu.Trigger>Account</DropdownMenu.Trigger>
  <DropdownMenu.Content>
    <DropdownMenu.Item onSelect={() => navigate('/profile')}>Profile</DropdownMenu.Item>
  </DropdownMenu.Content>
</DropdownMenu>
```

### Icon-only trigger

```tsx
<DropdownMenu>
  <DropdownMenu.Trigger aria-label="User menu">
    <UserIcon aria-hidden="true" />
  </DropdownMenu.Trigger>
  <DropdownMenu.Content>
    <DropdownMenu.Item onSelect={signOut}>Sign out</DropdownMenu.Item>
  </DropdownMenu.Content>
</DropdownMenu>
```

### Disabled items

```tsx
<DropdownMenu>
  <DropdownMenu.Trigger>Actions</DropdownMenu.Trigger>
  <DropdownMenu.Content>
    <DropdownMenu.Item onSelect={publish}>Publish</DropdownMenu.Item>
    <DropdownMenu.Item onSelect={archive} disabled>Archive (unavailable)</DropdownMenu.Item>
  </DropdownMenu.Content>
</DropdownMenu>
```

Disabled items get `aria-disabled="true"` and are skipped by keyboard navigation.

## API

### DropdownMenu (root)

<PropsTable
  rows={[
    { name: 'open', type: 'boolean', description: 'Controlled open state. Uncontrolled when omitted.' },
    { name: 'onOpenChange', type: '(open: boolean) => void', description: 'Called when open state changes. Required when open is provided.' },
    { name: 'children', type: 'ReactNode', required: true, description: 'Must contain one Trigger and one Content.' },
  ]}
/>

### DropdownMenu.Trigger

Requires exactly one of `children`, `aria-label`, or `aria-labelledby`.

<PropsTable
  rows={[
    { name: 'children', type: 'AccessibleText', description: 'Visible label text.' },
    { name: 'aria-label', type: 'AccessibleText', description: 'Invisible label for icon-only triggers.' },
    { name: 'aria-labelledby', type: 'string', description: 'ID of an external label element.' },
    { name: 'disabled', type: 'boolean', description: 'Disables the trigger.' },
    { name: 'className', type: 'string', description: 'Additional CSS class on the button.' },
  ]}
/>

### DropdownMenu.Content

<PropsTable
  rows={[
    { name: 'children', type: 'ReactNode', required: true, description: 'Items, separators, and sub groups.' },
    { name: 'className', type: 'string', description: 'Additional CSS class on the menu panel.' },
  ]}
/>

### DropdownMenu.Item

<PropsTable
  rows={[
    { name: 'onSelect', type: '() => void', required: true, description: 'Called when the item is activated.' },
    { name: 'children', type: 'ReactNode', required: true, description: 'Item label.' },
    { name: 'disabled', type: 'boolean', description: 'Marks the item aria-disabled and excludes it from keyboard navigation.' },
    { name: 'className', type: 'string', description: 'Additional CSS class on the item element.' },
  ]}
/>

### DropdownMenu.Separator

<PropsTable
  rows={[
    { name: 'className', type: 'string', description: 'Additional CSS class on the separator.' },
  ]}
/>

### DropdownMenu.Sub

<PropsTable
  rows={[
    { name: 'children', type: 'ReactNode', required: true, description: 'Must contain one SubTrigger and one SubContent.' },
  ]}
/>

### DropdownMenu.SubTrigger

Same accessible-name contract as `Trigger`.

<PropsTable
  rows={[
    { name: 'disabled', type: 'boolean', description: 'Disables the sub-trigger.' },
    { name: 'className', type: 'string', description: 'Additional CSS class.' },
  ]}
/>

### DropdownMenu.SubContent

<PropsTable
  rows={[
    { name: 'children', type: 'ReactNode', required: true, description: 'Sub-menu items.' },
    { name: 'className', type: 'string', description: 'Additional CSS class on the sub-menu panel.' },
  ]}
/>

## Keyboard

### Root menu

<KeyTable
  rows={[
    { keys: ['Enter'], action: 'Open menu and focus first item' },
    { keys: ['Space'], action: 'Open menu and focus first item' },
    { keys: ['Arrow Down'], action: 'Focus next item' },
    { keys: ['Arrow Up'], action: 'Focus previous item' },
    { keys: ['Home'], action: 'Focus first item' },
    { keys: ['End'], action: 'Focus last item' },
    { keys: ['Escape'], action: 'Close menu, return focus to trigger' },
    { keys: ['Tab'], action: 'Close menu, move focus naturally' },
    { keys: ['A-Z'], action: 'Typeahead: jump to first item starting with that character' },
  ]}
/>

### Submenu

<KeyTable
  rows={[
    { keys: ['Arrow Right'], action: 'Open submenu from SubTrigger' },
    { keys: ['Arrow Left'], action: 'Close submenu, return focus to SubTrigger' },
    { keys: ['Escape'], action: 'Close submenu, return focus to SubTrigger' },
  ]}
/>

The trigger position is computed once when the menu opens. Any scroll or resize event closes the menu.

## Do

<Do title="Always provide an accessible name on the trigger">
```tsx
<DropdownMenu.Trigger>Account</DropdownMenu.Trigger>
<DropdownMenu.Trigger aria-label="User menu">{undefined}</DropdownMenu.Trigger>
```
</Do>

<Do title="Always provide onSelect on every Item">
```tsx
<DropdownMenu.Item onSelect={() => navigate('/profile')}>Profile</DropdownMenu.Item>
```
</Do>

<Do title="Use disabled items to signal unavailable actions">
```tsx
<DropdownMenu.Item onSelect={archive} disabled>Archive (unavailable)</DropdownMenu.Item>
```
</Do>

## Don't

<Dont title="Render a trigger with no accessible name">
```tsx
<DropdownMenu.Trigger />  {/* compile error */}
<DropdownMenu.Trigger>{""}</DropdownMenu.Trigger>  {/* compile error */}
```

Both are TypeScript compile errors.
</Dont>

<Dont title="Omit onSelect">
```tsx
<DropdownMenu.Item>Profile</DropdownMenu.Item>  {/* compile error */}
```

`onSelect` is required. An item without an action has no purpose in a menu.
</Dont>

<Dont title="Render an empty Content">
```tsx
<DropdownMenu.Content>{false}</DropdownMenu.Content>
```

Fires a dev overlay with WCAG 1.3.1.
</Dont>
